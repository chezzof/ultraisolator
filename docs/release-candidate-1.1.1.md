# Release Candidate 1.1.1 Evidence

This note captures the local release-candidate gate for `v1.1.1`. It prepares
the GitHub Release handoff, but it does not publish a GitHub Release.

## Artifact Set

Generated by `powershell -ExecutionPolicy Bypass -File scripts\release-check.ps1`
on 2026-07-06:

| Artifact | Size |
|----------|------|
| `Esports Isolator PRO Setup 1.1.1.exe` | 301,357,636 bytes |
| `Esports-Isolator-PRO-1.1.1-portable.exe` | 301,141,759 bytes |
| `SHA256SUMS.txt` | 211 bytes |

## Candidate SHA256SUMS.txt

These hashes describe the local release-candidate build captured by this note.
Use the `SHA256SUMS.txt` asset attached to the published GitHub Release for
final download verification. For `v1.1.1`, GitHub normalized the public
installer asset name to `Esports.Isolator.PRO.Setup.1.1.1.exe`.

```text
db91ad911dfe604074bf4c0e57217de84e0354026d2147691add0631d272fd1d  Esports Isolator PRO Setup 1.1.1.exe
a725ffd2c62776e6d3a8a72ee03da55a8bae1b275e1f3e56d59e8cd12d70fcbc  Esports-Isolator-PRO-1.1.1-portable.exe
```

Checksums detect artifact corruption. They do not authenticate publisher
provenance without signed checksums, Sigstore, GitHub attestations, or code
signing.

## Verification

Commands run from a clean `chezzof/main` worktree on branch
`codex/release-candidate-publication-prep`:

- `npm.cmd --prefix ui ci` - passed; 0 vulnerabilities reported during install.
- `npm.cmd --prefix ui audit` - passed; 0 vulnerabilities.
- `python -m unittest discover -s tests -p "test_*.py" -v` - passed; 251 tests.
- `powershell -ExecutionPolicy Bypass -File scripts\verify.ps1` - passed.
- `npm.cmd --prefix ui run build:renderer` - passed.
- `npm.cmd --prefix ui run smoke` - passed.
- `npm.cmd --prefix ui run test:ui-quality` - passed; 4 visual and 3 a11y tests.
- `npm.cmd --prefix ui run build` - passed after hydrating Electron `v42.2.0` with `npm.cmd --prefix ui exec electron -- --version`.
- `npm.cmd --prefix ui run verify:packaged-runtime` - passed.
- `npm.cmd --prefix ui run verify:installed-artifacts` - passed.
- `powershell -ExecutionPolicy Bypass -File scripts\release-check.ps1` - passed and wrote `ui/dist-packaged/SHA256SUMS.txt`.
- `git diff --check` - passed.

## Release Notes Requirements

Before publishing, the GitHub Release notes must state:

- Windows 10/11 only.
- Administrator elevation is required for full tuning behavior.
- The installer is unsigned and may trigger Windows SmartScreen.
- Packaged builds require a trusted absolute `EII_PYTHON` interpreter path.
- Background jailing is opt-in.
- Anti-cheat compatibility boundaries are respected; this project must not be
  used to bypass anti-cheat software.
- `SHA256SUMS.txt` should be uploaded with the installer and portable artifacts.

Remaining manual presentation item: upload `docs/social-preview.png` through
GitHub repository Settings before broad public sharing.
